<h1>All Items for sale!</h1>

  <% @items.each do |item| %>
    <p>
      <%= link_to item.name, item_path(item.id) %>, $ <%= item.formatted_price %>
    </p>
    <p>
      <%= item.description %>
    </p>

<% end %>
